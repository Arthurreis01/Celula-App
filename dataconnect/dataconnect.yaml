specVersion: "v1beta"
serviceId: "celula-webapp"
location: "us-central1"
schema:
  source: "./schema"
  datasource:
    postgresql:
      database: "fdcdb"
      cloudSql:
        instanceId: "celula-webapp-fdc"
      # schemaValidation: "COMPATIBLE"
connectorDirs: ["./connector"]
